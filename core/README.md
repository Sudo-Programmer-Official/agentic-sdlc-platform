# Core Domain

Houses the SDLC state machine, approval gates, action ledger, and domain models.
Kept framework-agnostic for use by the API and agent layers.

## Packaging
Python package lives under `core/src/core`.
Install for local development:

```
pip install -e core
```
